#!/usr/bin/env ruby

puts 'App starting...'

require_relative '../config/environment'

puts "* Env #{Application.environment}"

puts "Ctrl-C to stop"

begin
  loop { sleep 3 }
rescue => Interrupt
  puts 'Stopped'
end
